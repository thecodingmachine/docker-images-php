#syntax=docker/dockerfile-upstream:1.4
ARG INSTALL_CRON=1
ARG INSTALL_COMPOSER=1
ARG PHP_VERSION_MINOR
ARG GLOBAL_VERSION
ARG REPO="thecodingmachine/php"
ARG TAG_PREFIX=""
ARG FROM_IMAGE="${REPO}:${TAG_PREFIX}${PHP_VERSION_MINOR}-${GLOBAL_VERSION}-slim-cli"
FROM $FROM_IMAGE
ARG TARGETOS
ARG TARGETARCH
